export const metadata = {
    title: "Sign Up - PlantHealth",
    description: "Create a PlantHealth account to start protecting your plants with AI.",
};

export default function SignUpLayout({ children }) {
    return <>{children}</>;
}
