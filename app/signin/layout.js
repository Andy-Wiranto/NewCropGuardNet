export const metadata = {
    title: "Sign In - PlantHealth",
    description: "Sign in to your PlantHealth account to continue detecting plant diseases.",
};

export default function SignInLayout({ children }) {
    return <>{children}</>;
}
